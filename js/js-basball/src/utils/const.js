const NUMBERS_LENGTH = 3;
const MIN_NUMBER = 1;
const MAX_NUMBER = 9;

const BASEBALL = {
  NOTHING: '낫싱',
  STRIKE: '스트라이크',
  BAll: '볼',
};

const GAME_RESULT = ['nothing', 'strike', 'ball'];

const GAME = {
  RESTART: '1',
  QUIT: '2',
};

module.exports = {
  NUMBERS_LENGTH,
  MIN_NUMBER,
  MAX_NUMBER,
  BASEBALL,
  GAME_RESULT,
  GAME,
};
